import React from "react";
import "../styles/Loader.css"; // You can style the loader using this CSS file

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
